function
v0 = p0 + $0    // v0 = x (Copia p0 para v0)
v1 = v0 * $3    // v1 = x * 3
v2 = v1 + $1    // v2 = (x * 3) + 1
v3 = v2 * $1    // v3 = v2 * 1 (Teste redundante para garantir fluxo)
ret v3
end